info:
  name: Get Users
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8787/api/users?limit=10
  headers:
    - name: Accept
      value: application/json
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
